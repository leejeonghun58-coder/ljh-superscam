# 월간 발주계획 시스템 디자인 명세

## 1. 디자인 방향

참고 HTML처럼 밝고 정돈된 업무용 대시보드로 설계한다. 화면은 장식보다 정보의 우선순위와 빠른 비교에 집중하며, 사용자가 현재 발주 상태를 한눈에 파악하고 필요한 행을 바로 찾을 수 있어야 한다.

- 기본 언어: 한국어
- 분위기: 신뢰감 있는 밝은 라이트 UI
- 핵심 인상: 정확함, 안정감, 빠른 판단
- 레이아웃: 좌측 고정 사이드바 + 상단 헤더 + 넓은 콘텐츠 영역
- 스타일: 순수 CSS, 둥근 카드, 얕은 그림자, 넉넉한 여백
- 차트 라이브러리: 사용하지 않음

## 2. 화면 구조

```text
┌──────────────────────────────────────────────────────────────┐
│ 사이드바 │ 상단 헤더: 화면명 · 기준월 · 사용자 · 알림        │
│          ├───────────────────────────────────────────────────│
│          │ 페이지 제목 / 설명                                │
│          │ [핵심 지표 카드] [핵심 지표 카드] [핵심 지표 카드]  │
│          │ [필터 / 검색 / 새로고침]                           │
│          │                                                    │
│          │ 발주계획 테이블                                    │
│          │ 상태 · 페이지네이션                                │
└──────────────────────────────────────────────────────────────┘
```

### 사이드바

- 상단에 `한국후지필름BI` 워드마크와 시스템명 표시
- 메뉴 그룹은 `대시보드`, `분석`, `발주계획`, `기준정보` 순서
- 현재 메뉴는 연한 파란 배경과 진한 텍스트로 표시
- 하단에는 로그인 사용자와 설정 메뉴 배치
- 데스크톱 기준 너비는 240px, 모바일에서는 상단 메뉴 버튼으로 접는다

### 상단 헤더

- 좌측: 현재 메뉴명과 짧은 경로
- 우측: 기준월 선택, 알림 아이콘, 사용자명
- 헤더는 콘텐츠와 구분되는 흰색 배경과 하단 경계선 사용

## 3. 색상 토큰

| 용도 | 색상 | 사용처 |
|---|---|---|
| 페이지 배경 | `#F6F8FB` | 전체 작업 영역 |
| 카드 배경 | `#FFFFFF` | 카드, 표, 필터 영역 |
| 기본 텍스트 | `#172033` | 제목, 주요 수치 |
| 보조 텍스트 | `#697386` | 설명, 단위, 메타정보 |
| 경계선 | `#E6EAF0` | 카드와 표 구분 |
| 주 색상 | `#2563EB` | 활성 메뉴, 주요 버튼, 링크 |
| 주 색상 연함 | `#EAF2FF` | 활성 상태 배경 |
| 정상 | `#15803D` | 정상, 완료, 충분 |
| 주의 | `#B45309` | 확인 필요, 부족 임박 |
| 위험 | `#DC2626` | 부족, 지연, 오류 |
| 중립 | `#64748B` | 미확정, 데이터 없음 |

## 4. 타이포그래피

- 기본 글꼴: `Pretendard`, `Noto Sans KR`, sans-serif
- 페이지 제목: 28px / 700
- 섹션 제목: 18px / 700
- 카드 수치: 28px / 700
- 본문: 14px / 400
- 표 헤더: 12px / 600
- 보조 문구: 12px / 400
- 숫자는 가독성을 위해 `font-variant-numeric: tabular-nums` 적용

## 5. 핵심 컴포넌트

### 페이지 제목 영역

페이지 제목 아래에 한 줄 설명을 둔다.

예시:

> 월간 발주계획
> 기준월의 수요와 재고를 바탕으로 산출된 법인별 발주 현황입니다.

### KPI 카드

첫 화면에는 3~4개의 핵심 지표만 노출한다.

- 총 대상 품목
- 발주 필요 품목
- 재고 부족 위험 품목
- 데이터 확인 필요 건수

카드는 `metric-label`, `metric-value`, `metric-foot` 구조를 사용한다. 수치만 크게 보이고, 단위와 기준은 수치 아래에 작게 표시한다.

### 필터 영역

- 기준월
- 생산법인
- 품목 또는 품목명 검색
- 발주 상태
- `초기화`, `조회` 버튼

필터는 표 위 한 줄에 배치하고, 화면이 좁아지면 두 줄로 자연스럽게 내려간다. 선택된 필터는 태그 형태로도 보여준다.

### 발주계획 표

권장 컬럼 순서:

| 컬럼 | 설명 |
|---|---|
| 상태 | 발주 필요, 정상, 확인 필요 |
| 품목코드 | 고정 폭의 코드 |
| 품목명 | 가장 넓은 영역 |
| 생산법인 | 법인명과 지역 배지 |
| 현재고 | 수량과 단위 |
| 예상 소진일 | 계산 가능한 경우에만 표시 |
| 권장 발주량 | 강조 수치 |
| 갱신일 | 마지막 데이터 기준 |

표 원칙:

- 헤더는 연한 회색 배경과 작은 대문자형 라벨 사용
- 행 높이는 56~64px로 유지
- 숫자는 우측 정렬, 텍스트는 좌측 정렬
- 행 hover 시 배경만 살짝 변경
- 중요한 상태는 색상만으로 구분하지 말고 텍스트 배지도 함께 표시
- 데이터가 없거나 계산할 수 없으면 `-` 대신 사유를 표시한다
  - `사용 이력 없음`
  - `리드타임 없음`
  - `기준 데이터 확인 필요`

## 6. 상태 배지

| 상태 | 배지 | 의미 |
|---|---|---|
| 발주 필요 | 빨간색 | 발주량이 산출되었고 조치가 필요함 |
| 주의 | 주황색 | 소진 임박 또는 일부 데이터 확인 필요 |
| 정상 | 초록색 | 현재 기준으로 조치가 필요하지 않음 |
| 미확정 | 회색 | 계산에 필요한 데이터가 부족함 |

배지 문구는 짧게 유지하고, 색상은 `tag`, `tag green`, `tag amber`, `tag gray` 클래스를 활용한다.

## 7. 반응형 규칙

- 1200px 이상: 사이드바, KPI 4열, 표 전체 표시
- 768~1199px: KPI 2열, 표 가로 스크롤
- 767px 이하: 사이드바 접기, KPI 1열, 필터 세로 배치
- 모바일 표는 핵심 컬럼만 우선 노출하고 상세 정보는 행 상세 영역에서 보여준다
- 고정 헤더를 사용할 경우 콘텐츠를 가리지 않도록 상단 여백을 확보한다

## 8. 상태별 화면 문구

### 조회 실패

`조회에 실패했습니다: {오류 메시지}`

재시도 버튼과 함께 원인을 확인할 수 있도록 표시한다.

### 빈 결과

`표시할 데이터가 없습니다.`

선택한 필터를 초기화할 수 있는 버튼을 함께 제공한다.

### 부분 데이터

`일부 항목은 계산에 필요한 데이터가 없어 확인이 필요합니다.`

숫자를 임의의 기본값으로 채우지 않고, 해당 행에 사유 코드를 표시한다.

## 9. 구현 원칙

- 새 화면은 `app/analysis/<기능이름>/page.tsx`에 둔다.
- 타입과 정규화 함수는 `lib/scm-model.ts`에 둔다.
- Supabase 조회 함수는 `lib/scm.ts`에 둔다.
- 화면 컴포넌트에는 평균, 분위수 등 계산식을 넣지 않는다.
- 화면은 원칙적으로 `analytics` 스키마만 조회한다.
- 기존 `app/globals.css`의 공통 클래스를 우선 사용하고, 부족한 스타일만 파일 끝에 추가한다.
- 숫자 계산 불가 상태는 `null`과 사유 코드로 표현한다.
- 페이지는 동적 조회를 위해 필요할 때 `dynamic = 'force-dynamic'`을 사용한다.

## 10. 완료 기준

- 첫 화면에서 기준월, 핵심 지표, 발주 상태를 5초 안에 파악할 수 있다.
- 표 행 수가 조회 결과 건수와 일치한다.
- 조회 오류와 빈 결과가 서로 다른 문구로 표시된다.
- 계산 불가 데이터가 임의의 숫자로 표시되지 않는다.
- 데스크톱과 모바일에서 가로 잘림 없이 사용할 수 있다.
- `npm run build`가 통과한다.
