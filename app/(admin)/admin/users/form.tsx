'use client';
import { useActionState } from 'react';
import { updateUserAccess, type UserActionState } from './actions';
export default function UserAccessForm({ userId, role, active, disabled }: { userId: string; role: 'ADMIN' | 'USER'; active: boolean; disabled: boolean }) { const [state, action, pending] = useActionState<UserActionState, FormData>(updateUserAccess, { error: null }); return <form action={action} className="user-access-form"><input type="hidden" name="user_id" value={userId} /><select name="role" defaultValue={role} disabled={disabled || pending} aria-label="role"><option value="USER">USER</option><option value="ADMIN">ADMIN</option></select><input type="hidden" name="active" value="false" /><label className="active-toggle"><input type="checkbox" name="active" value="true" defaultChecked={active} disabled={disabled || pending} /> 활성</label><button className="ui-button ui-button-secondary" disabled={disabled || pending}>{pending ? '저장 중…' : '저장'}</button>{state.error && <span className="auth-error" role="alert">{state.error}</span>}</form>; }