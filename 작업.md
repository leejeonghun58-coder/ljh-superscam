# 현재 프로젝트와 0910 다운로드 버전 비교 분석

## 결론

현재 프로젝트(`C:\SUPSERSCAM\superSCM-main`)가 기능 범위와 테스트 면에서 다운로드 버전(`C:\Users\fujifilm\Downloads\superSCM-main (0910)\superSCM-main`)보다 더 발전되어 있다.

따라서 다운로드 버전을 통째로 덮어쓰면 STEP 8/9, Forecast/Backtest, 관리자 기능, Agent 대화 저장 구조 등이 손실될 수 있다. 현재 프로젝트를 기준으로 유지하면서 다운로드 버전의 **0910 실데이터 대응**과 **Agent UX 개선**만 선택적으로 병합하는 것이 적절하다.

## 파일 비교 요약

| 구분 | 개수 |
|---|---:|
| 현재 프로젝트 파일 | 273 |
| 다운로드 버전 파일 | 168 |
| 동일 파일 | 114 |
| 내용이 다른 파일 | 46 |
| 현재 프로젝트에만 존재 | 113 |
| 다운로드 버전에만 존재 | 8 |

- 현재 프로젝트는 Git 저장소이며 비교 당시 작업 트리는 깨끗했다.
- 다운로드 폴더에는 `.git`이 없어 배포 또는 강의용 스냅샷으로 판단된다.
- `package.json`은 동일하다.

## 우선 수정 사항

### 1. 실데이터 Analytics View로 조회 구조 통일 — 최우선

다운로드 버전은 다음 실데이터 뷰를 기준으로 구현되어 있다.

- `analytics.v_shipment_trend`
- `analytics.v_item_demand_profile`
- `analytics.v_item_demand_kpi`
- `analytics.v_ol_accuracy`
- `analytics.v_ol_accuracy_fy`
- `analytics.v_bom_requirement_x`

현재 `lib/scm.ts`의 일부 Agent용 함수는 다음 문제가 생길 가능성이 있다.

- `getDemandProfile()`이 `core.fact_shipment`를 직접 조회한 뒤 ADI와 CV²가 이미 계산된 필드처럼 정규화한다.
- `getOlAccuracy()`가 `core.fact_mc_plan_actual`을 직접 조회한 뒤 WAPE와 Bias가 이미 계산된 필드처럼 정규화한다.
- 원본 fact 테이블에 집계 컬럼이 없다면 이 값들은 대부분 `null`이 될 수 있다.
- 화면과 Agent가 서로 다른 데이터 소스를 사용하게 된다.

권장 작업:

1. 다운로드 버전의 실데이터 타입과 정규화 함수를 `lib/scm-model.ts`에 선택적으로 병합한다.
2. 실데이터 Analytics View 조회 함수를 `lib/scm.ts`에 추가한다.
3. Agent Tool도 동일한 `lib/scm.ts` 함수를 사용하게 한다.
4. 현재의 Forecast, Backtest, 재고 관련 함수는 유지한다.

다운로드 버전의 `lib/scm.ts`로 전체 교체하면 안 된다.

### 2. 분석 화면을 0910 데이터 구조에 맞게 조정

다운로드 버전의 주요 화면 변경은 다음과 같다.

- Demand Profile: `v_sku_demand_profile` 대신 `v_item_demand_profile` 사용
- Model Comparison: Forecast 비교 대신 실제 OL 정확도 표시
- Lead Time: 실데이터 부재 안내 화면 표시
- Stockout: 재고 Snapshot과 MOQ 부재 안내 화면 표시
- Dashboard: `Lead Time · Stockout`보다 `Demand Profile · OL Accuracy` 중심으로 변경

현재 Supabase가 동일한 0910 실데이터를 사용한다면 이 구조로 맞추는 것이 적절하다. 단, 현재 DB에 검증된 Lead Time 및 재고 데이터가 있다면 관련 화면을 무조건 비활성화하지 말고 실제 View 존재 여부와 데이터 건수를 먼저 확인해야 한다.

### 3. Agent 실행 구조 보완

다운로드 버전에서 선택적으로 가져올 가치가 높은 기능:

- 한국어 System Prompt
- LLM token usage 수집 및 저장
- Structured Output fallback 상태를 다음 라운드까지 유지
- 계획, Tool 실행, 답변 작성, 검증 단계의 진행 상태 이벤트
- 전체 60초 제한에 사용할 외부 `AbortSignal`
- 누락된 환경변수 이름 반환
- Tool 결과의 `data_as_of`를 최종 답변에 반영

현재 LLM fallback은 실패한 형식을 기억하지만 orchestrator가 fallback 발생 사실을 명확히 전달받지 못한다. `fellBackToJsonObject` 같은 상태를 반환하고 남은 라운드도 낮은 형식으로 보내도록 보완하는 것이 좋다.

### 4. Agent 화면 UX 추가

다운로드 버전에만 있는 주요 구현:

- `app/(user)/agent/answer-card.tsx`
- `app/api/agent/stream/route.ts`

추가할 만한 기능:

- SSE 기반 진행 상태 표시
- 계획 → 조회 → 설명 → 검증 단계 표시
- 다중 턴 대화 말풍선
- 사용자 스크롤을 고려한 자동 스크롤
- 중단 시 `AbortController` 정리
- Guardrail 검증이 끝난 답변만 출력
- 대화 저장 실패 경고
- 질문 길이 500자 제한
- API Key가 아닌 누락 환경변수 이름만 표시
- `prefers-reduced-motion` 접근성 지원

이 기능은 API Route, orchestrator, auth, CSS를 함께 변경해야 한다. `chat-form.tsx`만 복사해서는 정상 동작하지 않는다.

### 5. Agent Schema는 호환 방식으로 확장

현재 프로젝트 Schema의 장점:

- 고정된 verdict 코드
- 잘못된 JSON과 필드 누락 거부
- 출처 없는 숫자 검증
- 계산 불가 응답 변환

다운로드 버전 Evidence의 유용한 필드:

- `label`
- `value`
- `unit`
- `source_tool`

현재 strict validation은 유지하면서 `unit`, `source_tool`을 추가하는 방식이 적절하다. Parser를 느슨하게 바꾸거나 verdict를 자유 문자열로 바꾸는 것은 권장하지 않는다.

Risk 코드는 공통 Badge와 연결하기 위해 다음 값으로 정리할 수 있다.

- `SAFE`
- `WARNING`
- `CRITICAL`
- `CALCULATION_UNAVAILABLE`

단, Schema, orchestrator, UI, 테스트를 함께 변경해야 한다.

### 6. Guardrail은 현재 구조 유지

현재 Guardrail은 `toolName.key` 형태로 숫자 출처를 관리하므로 다운로드 버전보다 출처 검증이 엄격하다.

다운로드 버전처럼 모든 숫자를 단순 배열로 평탄화하면 의미가 다른 숫자를 오용할 수 있다. 예를 들어 MOQ `100`을 `100일`로 표현해도 숫자만 같으면 통과할 위험이 있다.

현재 구조를 유지하면서 다음을 보완하는 것이 좋다.

- 검증한 숫자 목록
- 위반 숫자 목록
- 재생성 여부
- 사용자 질문에 포함된 숫자의 허용 정책
- Guardrail 결과의 대화 저장 및 trace 표시

### 7. 대화 저장 Migration은 현재 버전 유지

현재 프로젝트의 대화 저장 구조가 기존 요구사항에 더 잘 부합한다.

- 질문과 답변을 RPC 한 transaction으로 저장
- `role`, `content`, `answer`, `tool_trace`, `usage`, `guardrail` 저장
- 본인 대화만 접근
- ADMIN 전체 조회
- anon 접근 차단

다운로드 migration은 대화와 메시지를 분리 저장하므로 완전한 transaction 보장이 약하다. 따라서 현재 migration을 교체하면 안 된다.

참고할 부분은 기존에 다른 구조의 Agent 테이블이 있을 경우 `_legacy_<timestamp>` 형태로 보존하는 호환성 사전 검사와 주석·문서화 정도다.

## 추가 검토 대상 파일

다운로드 버전에서 선택적으로 가져올 수 있는 파일:

- `components/analysis/no-realdata-notice.tsx`
- `components/analysis/ol-accuracy-table.tsx`
- `app/(user)/agent/answer-card.tsx`
- `app/api/agent/stream/route.ts`

추가로 반영할 내용:

- Agent 관련 `ARCHITECTURE.md`, `SCHEMA.md` 문서
- `.env.example`의 주석 처리된 `OPENAI_*` 예시
- `.gitignore`의 강의 자료 폴더 제외 규칙

현재 `tsconfig`의 ES2018 설정은 유지한다. 다운로드 버전의 ES5 설정으로 낮추지 않는다.

## 유지해야 하는 현재 프로젝트 기능

다음 항목은 다운로드 버전에 맞춘다는 이유로 삭제하거나 덮어쓰지 않는다.

- STEP 8/9 관련 기능
- Forecast 및 Backtest 저장 구조
- 관리자 화면과 RBAC
- Python Forecast Service 관련 파일
- 현재의 엄격한 Agent JSON 검증
- `toolName.key` 기반 숫자 Guardrail
- transaction 기반 Agent 대화 저장 RPC
- 기존 테스트와 추가 migration

## 권장 구현 순서

1. 실제 Supabase에서 0910 Analytics View와 컬럼 존재 여부를 확인한다.
2. `lib/scm-model.ts`에 실데이터 타입과 정규화 함수를 추가한다.
3. `lib/scm.ts`에 실데이터 Analytics View 조회 함수를 추가한다.
4. Agent Tool을 같은 조회 함수에 연결한다.
5. Demand Profile 및 OL Accuracy 화면을 변경한다.
6. Lead Time 및 Stockout 데이터 존재 여부에 따라 안내 화면을 적용한다.
7. LLM fallback 상태, usage, System Prompt를 보완한다.
8. SSE Agent UI와 저장 실패 경고를 구현한다.
9. Agent Schema와 Evidence를 호환 방식으로 확장한다.
10. 테스트, TypeScript 검사, 빌드를 실행한 뒤 단계별로 커밋한다.

## 주의사항과 알려진 위험

- 코드 변경 전에 대상 Supabase에 실데이터 Analytics View가 실제로 존재하는지 확인해야 한다.
- 다운로드 migration을 현재 DB에 그대로 적용하면 기존 Agent 테이블 및 RPC와 충돌할 수 있다.
- 다운로드 폴더는 Git 이력이 없는 스냅샷이므로 현재 프로젝트와 직접적인 선후 관계를 확정할 수 없다.
- 다운로드 버전의 일부 journal 문서는 실제 Tool 이름과 일치하지 않아 문서가 최신 상태가 아닐 가능성이 있다.
- 다운로드 버전 전체 복사 대신 기능별 테스트를 먼저 추가한 후 선택적으로 병합해야 한다.

## 최종 방향

현재 프로젝트를 기준 저장소로 유지한다. 다운로드 버전에서는 다음 두 범위만 우선 이식한다.

1. 0910 실데이터 Analytics View와 일치하는 조회·화면 계약
2. SSE 진행 상태, 다중 턴 화면, 저장 경고를 포함한 Agent UX

현재 프로젝트가 이미 더 강하게 구현한 Forecast/Backtest, 보안, Guardrail, transaction 기반 대화 저장은 그대로 보존한다.
